{
  "name": "marieta-sync-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker que recebe payload do Menudino vindo do bookmarklet e grava no Firestore com service account. Desacopla o bookmarklet (que roda na aba do Menudino) do Firebase Auth (que bloqueia CORS a partir de menudino.com).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
